#pragma once

namespace mpm {
	struct MaterialPoint;
	struct PointCloud;
	struct GridNode;
	struct Grid;

	namespace control {
		struct ControlPoint;
		struct ControlGridNode;
		struct SpaceTimeComputationGraph;
		struct ControlPointCloud;
		struct ControlGrid;
	}
}